enum SessionStatus {
  none,
  started,
  finish,
}

enum VerifyStatus { none, number, code, newPassword, done, error }


// Define un enum con los tipos de tarjetas
enum InfoContextType { error, warning, info, success }

enum AlertType { info, success, error, warning }